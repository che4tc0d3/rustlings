// functions1.rs
//
// Execute `rustlings hint functions1` or use the `hint` watch subcommand for a
// hint.


//The problem was that we call a function that dosn't exist so to fix it you just need to create the function  
fn call_me(){}

fn main() {
    call_me();
}
